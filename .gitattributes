# Shell scripts must use LF or they break on macOS/Linux (bad interpreter).
*.sh text eol=lf

# Windows scripts use CRLF.
*.bat text eol=crlf
*.ps1 text eol=crlf

# Treat the PDF as binary (no EOL conversion).
*.pdf binary
